# server（占位）

后期 Node.js API 放在本目录。H5 与微信小程序共用这一套后端。

尚未实现。接口形状、事务和迁移规则见仓库根目录：

`.cursor/skills/myhome-backend-contracts/`

建议先读 `capability-map.md`（能力与领域）和 `data-and-api.md`（表与接口），再读 `account-model.md`、`backend-contracts.md`。
